# Replica count increased to 5 instances
replicaCount: 5

# Needed for MiniKube when using local Docker images
image:
  pullPolicy: Never

# Custom configuration for MiniKube Prometheus Operator
prometheus:
  enabled: true
  namespace: monitor
  serviceAccount: monitor-prometheus-operato-prometheus
